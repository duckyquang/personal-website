import { SectionHeading } from "@/components/portfolio/SectionHeading";
import { sectionWorkBuildNext } from "@/content/portfolio";

export default function WorkBuildNextPage() {
  return (
    <SectionHeading
      title={sectionWorkBuildNext.title}
      subtitle={sectionWorkBuildNext.subtitle}
    />
  );
}
