import { SectionHeading } from "@/components/portfolio/SectionHeading";
import { sectionResearchSynthica } from "@/content/portfolio";

export default function ResearchSynthicaPage() {
  return (
    <SectionHeading
      title={sectionResearchSynthica.title}
      subtitle={sectionResearchSynthica.subtitle}
    />
  );
}
