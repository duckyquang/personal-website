import { SectionHeading } from "@/components/portfolio/SectionHeading";
import { sectionResearchCs } from "@/content/portfolio";

export default function ResearchCsPage() {
  return (
    <SectionHeading
      title={sectionResearchCs.title}
      subtitle={sectionResearchCs.subtitle}
    />
  );
}
